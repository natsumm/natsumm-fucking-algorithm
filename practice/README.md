# Practice

每道题使用独立目录。自己练习的题目保留 `README.md` 和 `solution.py`；由 Coding Agent 初始化的新题目使用 `README.md`、`solution.py`（LeetCode 模板）和 `test_solution.py`（pytest 用例）三件套。

等级目录表示当前训练的核心能力，而不是题目的绝对难度。运行 `python3.12 tools/new_problem.py --help` 查看创建方式。
