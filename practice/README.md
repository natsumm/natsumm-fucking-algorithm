# Practice

每道题使用独立目录，目录名格式为 `<题目编号>_<中文题名>`，例如 `560_和为K的子数组`；无编号的题目（如华为 OD 机试题）直接使用题名，例如 `考勤信息`。

自己练习的题目保留 `README.md` 和 `solution.py`；由 Coding Agent 初始化的新题目使用 `README.md`、`solution.py`（LeetCode 模板）和 `test_solution.py`（pytest 用例）三件套。

运行 `python3.12 tools/new_problem.py --help` 查看创建方式。
